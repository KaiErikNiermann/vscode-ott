name: Release VS Code Extension

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Generate Langium artifacts
        run: pnpm run langium:generate

      - name: Build
        run: pnpm run build

      - name: Test
        run: pnpm run test

      - name: Package extension
        run: pnpm dlx @vscode/vsce package --no-dependencies
        working-directory: packages/extension

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v7
        with:
          name: vscode-ott-${{ github.ref_name }}
          path: packages/extension/*.vsix

      - name: Create or update GitHub Release
        run: |
          gh release upload "${{ github.ref_name }}" packages/extension/*.vsix --clobber 2>/dev/null \
            || gh release create "${{ github.ref_name }}" packages/extension/*.vsix --title "${{ github.ref_name }}" --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to VS Code Marketplace
        if: vars.PUBLISH_MARKETPLACE == 'true'
        run: pnpm dlx @vscode/vsce publish --no-dependencies --packagePath *.vsix
        working-directory: packages/extension
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      - name: Publish to Open VSX
        if: vars.PUBLISH_OPENVSX == 'true'
        run: pnpm dlx ovsx publish *.vsix
        working-directory: packages/extension
        env:
          OVSX_PAT: ${{ secrets.OVSX_PAT }}
